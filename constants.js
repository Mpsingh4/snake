const IP = '165.227.47.243';
const PORT = 50541;
const name = 'MPS';
const up = 'w';
const left = 'a';
const right = 'd';
const down = 's';

module.exports = {
  IP,
  PORT,
  name,
  up,
  left,
  right,
  down,
};

//defines constants for the IP address, port number, player name, and movement keys used in the game.
//Then exports constants for use in the other 3 files.